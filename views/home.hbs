<!-- Card -->
<div class="card" style="width: 18rem;">
    <!--<img class="card-img-top" src="..." alt="Card image cap"> -->
    <div class="card-body", id="cardBody">
        {{#each list}}
            <h5 class="card-title">{{this.title}}</h5>
            <p class="card-text">{{this.description}}</p>
            <p class = "card-text">{{this.price}}</p>
            <a href="#" class="btn btn-primary">Add to Cart</a>
            <a href="#" class="btn btn-primary">More</a>
        {{/each}}
    </div>
</div>

<!--
<script type="text/javascript">
    var posting = 10 // Used to ensure that we always have 10 posts on the front
    $(document).ready(function() {
            if(posting < 100) {
                posting++; //increment the count
                var spanIdName = "post" + posting; // create the id string
                $("#cardBody").append('<div class="form-group col-md-6">\n' +
                      </div>')
                console.log(spanIdName + " created");
            } // end of if statement
        });
</script>
-->